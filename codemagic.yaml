# Automatically generated on 2020-08-07 UTC from https://codemagic.io/app/5f2c1b6a8ff73d000f7bc1e1/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  master-merge:
    name: Master Workflow
    max_build_duration: 60
    environment:
      vars:
        FCI_FLUTTER_SCHEME: Production
        ANDROID_NDK_VERSION: 21.3.6528147
        ANDROID_SDK_VERSION: 30
        CODECOV_TOKEN: Encrypted(Z0FBQUFBQmZMUHJ6aU9za084Zi1Xd3RscnAzbGVsbFROTUhaMFBKejAzX2Q4b3dGQ1I3a1B6Y1FtY3lucmh1Z3FpZGhPbE1laTc0V1VIUndZTlpMYzF6T0VEWFdJWUwxZW9PeFJMQk9OS29yQUVWdzVqbHZzNXp3aE1Uc0ZOa09UQWF0WXg5cXZlNjk=)
      flutter: beta
      xcode: latest
      cocoapods: default
    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - $FCI_BUILD_DIR/ios/Pods
        - $FCI_BUILD_DIR/android/.gradle
    triggering:
      events:
        - push
        - pull_request
        - tag
      branch_patterns:
        - pattern: master
          include: true
          source: true
    scripts:
      - name: Setup local properties
        script: |
            # set up local properties
            echo "flutter.sdk=$HOME/programs/flutter" > "$FCI_BUILD_DIR/android/local.properties"

      - name: Flutter pub get
        script: make dependencies

      - name: Flutte Analysis
        script: make analyze

      - name: Flutter Unit test
        script: make unit-test

      - name: Codecov upload
        script: make codecov

      - name: Build
        script: |
          #!/bin/sh
          set -e # exit on first failed commandset
          set -x # print all executed commands to the log
          /usr/bin/plutil -replace CFBundleIdentifier -string com.example.app ios/Runner/Info.plist
          make build-prd-apk
          make build-prd-ipa

    artifacts:
      - build/**/outputs/**/*.apk
      - build/**/outputs/**/*.aab
      - build/**/outputs/**/mapping.txt
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - flutter_drive.log

    publishing:
      slack:
        channel: '#develop'
        notify_on_build_start: false
      github_releases:
        prerelease: false
        artifact_patterns:
          - 'app-production-release.apk'
          - 'Runner.ipa'